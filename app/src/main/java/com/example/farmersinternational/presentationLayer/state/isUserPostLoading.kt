package com.example.farmersinternational.States

data class isUserPostLoading(
    val isLoading:Boolean = false,
    //on Refresh each post will pop up
    //with the things on the top right
    //gmo, pastureRaised,GrassFed etc
    //
)
